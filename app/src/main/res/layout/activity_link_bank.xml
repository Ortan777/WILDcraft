<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_dark"
    android:padding="24dp">

    <!-- Top section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center_vertical">

        <!-- Icon + Title -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🏦"
            android:textSize="48sp"
            android:layout_marginBottom="12dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Link Bank Account"
            android:textColor="@color/text_primary"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="8dp"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Enter your bank server's IP address"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:layout_marginBottom="32dp"/>

        <!-- Currently linked info card -->
        <TextView
            android:id="@+id/tvBankInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@drawable/card_dark"
            android:padding="16dp"
            android:textColor="@color/accent_blue"
            android:textSize="14sp"
            android:lineSpacingExtra="4dp"
            android:visibility="gone"
            android:layout_marginBottom="24dp"
            android:text=""/>

        <!-- IP Input label -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Bank Server IP"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            android:layout_marginBottom="8dp"/>

        <!-- IP Input field -->
        <EditText
            android:id="@+id/etBankIp"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:background="@drawable/input_bg"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:hint="e.g. 192.168.1.5"
            android:textColor="@color/text_primary"
            android:textColorHint="@color/text_hint"
            android:textSize="15sp"
            android:inputType="text"
            android:layout_marginBottom="12dp"/>

        <!-- Status text -->
        <TextView
            android:id="@+id/tvLinkStatus"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="13sp"
            android:lineSpacingExtra="4dp"
            android:text=""/>

        <!-- Progress bar -->
        <ProgressBar
            android:id="@+id/progressLink"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:layout_marginTop="8dp"
            android:visibility="gone"/>

    </LinearLayout>

    <!-- Bottom buttons — pinned to bottom -->
    <Button
        android:id="@+id/btnConnectBank"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:text="Connect to Bank"
        android:textColor="@color/text_primary"
        android:textSize="15sp"
        android:background="@drawable/btn_primary"
        android:layout_marginBottom="12dp"/>

    <Button
        android:id="@+id/btnBackFromBank"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:text="Back"
        android:textColor="@color/text_secondary"
        android:textSize="15sp"
        android:background="@drawable/btn_outline"/>

</LinearLayout>